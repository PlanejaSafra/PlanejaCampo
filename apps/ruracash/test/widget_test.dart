import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('RuraCash smoke test', (WidgetTester tester) async {
    // Smoke test placeholder - requires Hive initialization
    expect(true, isTrue);
  });
}
